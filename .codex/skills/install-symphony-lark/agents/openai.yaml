interface:
  display_name: "Install Symphony Lark"
  short_description: "Clone, bootstrap Lark auth and tasklists, and run Symphony."
  default_prompt: "Use $install-symphony-lark to set up Symphony Lark in an explicit target path, including app bootstrap, tasklist creation, and Status setup."

policy:
  allow_implicit_invocation: true
