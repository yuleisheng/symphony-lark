interface:
  display_name: "Install Symphony Lark"
  short_description: "Clone, bootstrap Lark auth and tasklists, and run Symphony."
  default_prompt: "Use $install-symphony-lark to set up Symphony Lark in an explicit target path, including GitHub auth, SYMPHONY_REPO_ROOT setup, and app-driven tasklist bootstrap."

policy:
  allow_implicit_invocation: true
